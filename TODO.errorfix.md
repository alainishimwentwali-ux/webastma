## Error fix TODO

- [ ] Add missing `dev` script in `backend/package.json` so the root `npm run start` can run backend in dev mode.
- [ ] Run `npm run start` (or `npm run backend`) and verify backend starts and `GET /health` returns `{ status: 'ok' }`.
- [ ] If a different runtime error appears, capture it and apply the targeted fix.

